'use strict';

module.exports = {
  siteTitle: 'My Blog',
  siteDescription: '마크다운으로 쓰는 블로그',
  // Root-relative base path. Change to e.g. '/my_blog' if deployed under a subpath
  // (such as a GitHub Pages project page). Keep '' for a root domain deploy.
  basePath: '',
  nav: [
    { label: '홈', href: '/' },
    { label: '태그', href: '/tags/' },
  ],
};
