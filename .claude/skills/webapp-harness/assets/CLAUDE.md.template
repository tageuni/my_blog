# CLAUDE.md

## 작업 사이클
1. Plan — 서브에이전트를 만들어 spec.md를 작성시킨다. 사용자 승인을 받는다.
2. Work — 서브에이전트를 만들어 구현한다. 화면이 3개 이상이면 화면별로 서브에이전트를 나눈다.
3. Review — 서브에이전트를 만들어 테스트하고 review.md를 작성시킨다.
4. (선택) Gemini 2차 리뷰 — 다른 모델(Antigravity CLI 등)로 독립적인 리뷰를 한 번 더 받고, 타당한 지적만 반영한다.
5. Commit — 기능마다 깃 커밋하고 깃허브에 푸시한다.

## 서브에이전트 규칙
- 서브에이전트에게 작업을 넘길 때 전용 지침 파일(.md)을 만들어 전달한다.
- 서브에이전트는 지침 파일에 명시된 범위만 수정한다.
- Work가 복잡하면 여러 서브에이전트로 나눈다. 각각 독립적으로 작업할 수 있게 범위를 겹치지 않게 나눈다.

## 문서 파일 이름 규칙
- 첫 기능은 `spec.md`/`review.md`처럼 접미사 없이 써도 되지만, 이후 기능부터는 반드시 `spec-<feature>.md`/`review-<feature>.md`처럼 접미사를 붙여서 이전 기능 문서를 덮어쓰지 않게 한다.

## 규칙
- 승인 없이 구현을 시작하지 않는다.
- 막히면 사용자에게 알린다.
- 위험한 작업(강제 삭제, force push, 설정 파일 직접 수정 등)은 하지 않는다. `.claude/settings.json`의 deny 목록 참고.

<!--
이 파일은 webapp-harness 스킬의 템플릿이다. 프로젝트에 맞게 다듬을 것:
- 이 프로젝트의 실제 기술 스택(프레임워크, 빌드 도구 등)을 알고 있다면 "작업 사이클"이나 별도 섹션에 짧게 적어둔다.
- 사이클/서브에이전트 규칙 자체는 웬만하면 그대로 유지하는 걸 권장한다 — my_blog에서 검증된 형태다.
-->
